version: "2"

sql:
  - schema: "migrations"
    queries: "queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "../internal/database"
        overrides:
          - db_type: "jsonb"
            go_type:
              type: "[]byte"

